import 'package:flutter/material.dart';

class LecturerCoursesPage extends StatelessWidget {
  const LecturerCoursesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("My Courses List (In Progress)"));
  }
}